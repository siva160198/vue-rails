default: &default
  adapter: postgresql
  encoding: unicode
  max_connections: <%= ENV.fetch("RAILS_MAX_THREADS") { 5 } %>

development:
  <<: *default
  database: tourplan_development

# Warning: The database defined as "test" will be erased and
# re-generated from your development database when you run "rake".
# Do not set this db to the same as development or production.
test:
  <<: *default
  database: tourplan_test

production:
  primary: &primary_production
    <<: *default
    database: tourplan_production
    username: tourplan
    password: <%= ENV["TOURPLAN_DATABASE_PASSWORD"] %>
  cache:
    <<: *primary_production
    database: tourplan_production_cache
    migrations_paths: db/cache_migrate
  queue:
    <<: *primary_production
    database: tourplan_production_queue
    migrations_paths: db/queue_migrate
  cable:
    <<: *primary_production
    database: tourplan_production_cable
    migrations_paths: db/cable_migrate
